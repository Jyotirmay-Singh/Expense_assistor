# Copy to .env and adjust as needed. Never commit the real .env.

# Flask
SECRET_KEY=change-me-in-production

# Database — must match docker-compose.yml credentials for local dev
DATABASE_URL=postgresql+psycopg2://fincheck:fincheck@localhost:5544/fincheck
